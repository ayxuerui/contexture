## 1. Shared inlining helper

- [x] 1.1 `src/core/conventions.ts`: `inlineDocBody(doc: ScannedDoc, headingOffset: number): string[]`, beside `scanDocsDir`/`extractDocMetadata`. DEVIATION from the drafted signature: no separate `raw` parameter — `ScannedDoc` gained a `body` field (the frontmatter-stripped content `extractDocMetadata` already computed internally and previously discarded), so `inlineDocBody` reads `doc.body` directly and never re-reads or re-parses the file. Drops a leading `# ` heading when it duplicates `doc.title` (case-insensitive); demotes every remaining ATX heading by `headingOffset` levels, capped at H6; strips `contexture:<region>` fence marker lines via `GENERIC_START_RE`/`GENERIC_END_RE`, newly exported from `src/core/fs/fenced-region.ts` (the same primitive `validateFenceIntegrity` uses) rather than a second regex; trims trailing blank lines
- [x] 1.2 Superseded by 1.1's `body`-field design — nothing left to do
- [x] 1.3 `test/unit/conventions.test.ts`: `inlineDocBody` unit tests — frontmatter stripped (via `extractDocMetadata`'s `body`), duplicate H1 dropped, a non-matching H1 kept and demoted, heading demotion at offset 2 and offset 1, H6 cap, a nested `contexture:rollup` fence's markers stripped with body kept, byte-stable across repeated calls

## 2. Inline conventions

- [x] 2.1 `src/core/agents-doc.ts`: `renderConventionBlock(doc)` (new, exported for reuse by drift detection) renders `### <title>` + `inlineDocBody(doc, 2)` + `_Source: <path>_`; `renderConventionsSection` joins one block per scanned doc, blank-line separated; the empty-store branch (`store-conventions-empty.md`) unchanged in shape, wording updated (see 2.2)
- [x] 2.2 `templates/agents/store-conventions.md`: `__CONVENTION_INDEX__` → `__CONVENTION_BODIES__`, intro reworded to "inlined in full"; `templates/agents/store-conventions-empty.md`: reworded to "inlined here in full on regeneration" (the shared trailing paragraph kept byte-identical across both templates, "indexed" → "inlined" in both)
- [x] 2.3 `test/unit/conventions.test.ts` and `test/unit/agents-doc.test.ts`: populated-conventions exact-output blocks rewritten for inlined bodies + provenance lines; empty-store exact-output block and its byte-identical-last-paragraph drift guard updated for the new wording and kept passing
- [x] 2.4 `npx vitest run test/unit/conventions.test.ts test/unit/agents-doc.test.ts --exclude '**/.claude/worktrees/**'` green (58/58) — the `--exclude` matches this shared dev box's pre-existing stale-nested-worktree test-discovery contamination, orthogonal to this change (see `generalize-identity-migration-residue`'s final report)

## 3. Inline mission

- [x] 3.1 `src/core/agents-doc.ts`: `AGENTS_MD_MISSION_FENCE`, `renderMissionSection(config, missionRaw: string | null): string[]` (pure — the caller reads the file), `buildAgentsMissionSection(root, config)`. DEVIATION (real bug caught while writing tests): `upsertFencedRegionInFile` appends a fence pair unconditionally when absent, even with an empty body — so a naive `buildAgentsMissionSection` would have written an empty `<!-- >>> contexture:mission ... -->` pair into every store's `AGENTS.md`, mission configured or not. Fixed: when `renderMissionSection` returns `[]`, `buildAgentsMissionSection` calls `removeFencedRegionFromFile` instead (idempotent no-op when absent; also cleans up correctly if a store un-sets a previously-configured mission)
- [x] 3.2 `templates/agents/mission.md`: new — `## Mission` heading, `__MISSION_BODY__` token, trailing `_Source: __MISSION_PATH___` line (renders as `_Source: <path>_`)
- [x] 3.3 `src/core/reconcile.ts`: `buildAgentsMissionSection` registered in the builder loop; `src/commands/init.ts`: called directly in fixed section order (see task 5 — a fresh store has no reordering to do, so call order alone determines layout)
- [x] 3.4 Kept `renderMissionPointer` and the canonical section's pointer sentence — design.md's "identity/mission pointer coexists with the inlined Mission section" decision. Sentence reworded to end "...its full content follows in the \"Mission\" section below" (was "see the skill index below")
- [x] 3.5 `templates/agents/canonical.md`: confirmed no separate change needed for the pointer sentence itself
- [x] 3.6 `test/unit/agents-doc.test.ts`: mission-section present-when-configured / absent-when-unconfigured / absent-when-note-missing, nested-fence stripping, `buildAgentsMissionSection` writes and is convergent, mission-pointer-coexists-with-section exact-output case

## 4. Remove the skill index

- [x] 4.1 `templates/agents/canonical.md`: `### Skill index` heading, its intro, and `__SKILL_INDEX__` deleted
- [x] 4.2 `templates/agents/canonical.md`: identity paragraph's "(see the skill index below)" removed; "knowledge and procedures" also corrected to "knowledge and skills" for consistency with the rest of the document (a pre-existing wording inconsistency, fixed in passing since this line was already being touched)
- [x] 4.3 `src/core/agents-doc.ts`: `renderCanonicalSection` no longer takes a `skills` parameter or does a `__SKILL_INDEX__` substitution; `buildAgentsCanonicalSection` no longer awaits `scanSkills`
- [x] 4.4 Confirmed `syncShippedSkills` untouched and still called from `reconcileStore`/`init.ts` — skills are still shipped and refreshed on disk, only the `AGENTS.md` index of them is gone
- [x] 4.5 `docIndexEntry` deleted outright (nothing calls it after task 2's rewrite to `renderConventionBlock`) rather than patched for the embedded-newline bug — moot once deleted
- [x] 4.6 `test/unit/agents-doc.test.ts`, `test/unit/verify-command.test.ts`, `test/integration/owned-skills.test.ts`, `test/integration/adapters-and-entry-doc.test.ts`, `test/unit/update-command.test.ts`: every skill-index-entry assertion removed or rewritten

## 5. Section reordering

- [x] 5.1 `src/core/fs/fenced-region.ts`: `reorderFencedRegions(text, orderedFences): {text, changed, blocked}` (pure, operating on text directly — matching this file's existing `upsertFencedRegion`/`removeFencedRegion` shape rather than taking a file path) plus a file-backed `reorderFencedRegionsInFile`. Locates every fence from `orderedFences` present in the text; if the present ones (in their current file order) are separated only by blank lines, rewrites them in the given order, each fence's own body moved byte-for-byte, content before/after the whole block untouched; otherwise no change, `blocked: true`
- [x] 5.2 `src/core/reconcile.ts`: `AGENTS_MD_SECTION_ORDER` (canonical → mission → retrieval-leg-routing → capture-and-ingest → placement → store-conventions, exported from `agents-doc.ts`) reordered via `reorderFencedRegionsInFile` after the builder loop; its `changed` folded into `agentsChanged`. `blocked` is not consumed here — task 6.4's lint check recomputes it independently
- [x] 5.3 `test/unit/agents-doc.test.ts` (`reorderFencedRegionsInFile (via reconcileStore section order)` describe block): a fresh `ctxr init` writes sections in fixed order; a drifted-but-contiguous store converges via `reconcileStore`; hand-written content between two sections blocks reordering and is preserved verbatim

## 6. Drift detection

- [x] 6.1 `src/commands/verify.ts`: replaced the skill-index check with (a) every non-mission managed section present (fence non-empty), (b) inlined conventions/mission content matches source via the new shared `checkAgentsMdDrift` helper (`agents-doc.ts`) — factored out so `doctor` and `verify` can never disagree — and (c) following one skill by path (no index lookup)
- [x] 6.2 `src/core/checks/integrity-checks.ts`: `agentsMdInlinedContentCurrentCheck` (store-scope invariant) reuses `checkAgentsMdDrift`, naming every drifted convention file and the mission path when applicable. ADDED (not in the original plan, needed for real pre-commit-hook correctness): `stagedAgentsMdInlinedContentCurrentCheck` (staged-scope invariant) — the store-scope check reads the working tree, the wrong source of truth for a pre-commit hook (an operator could stage a convention edit without staging a regenerated `AGENTS.md`, and the working tree could still look "fixed" if they happened to run `ctxr update` first without `git add`ing the result); the staged variant reads `ctx.staged[].content` per touched path, falling back to disk for untouched ones, and fails when a staged convention/mission change has no matching staged `AGENTS.md` regeneration
- [x] 6.3 Confirmed `harnessEntryNoDuplicateConventionTextCheck` does not fire on inlined conventions — it compares `CLAUDE.md`'s content *outside* its own managed import fence against `AGENTS.md` headings; inlined conventions live inside `AGENTS.md`'s own `store-conventions` fence, which this check has always treated as canonical, not foreign duplication
- [x] 6.4 DEVIATION from the original task text ("wire into doctor's observation output"): `ctxr doctor` runs only `severity: 'invariant'` checks (`src/commands/doctor.ts`); an "observation, not a failure" is a `ctxr lint` finding by construction in this codebase (`severity: 'observation'`, run by `lint`). Fixed the delta spec, design.md, and this task list to say `lint`, not `doctor`, before implementing. `src/core/checks/organize-checks.ts`: `agentsMdSectionOrderBlockedCheck` (observation) calls `reorderFencedRegions` against `AGENTS.md`'s current content and reports when `blocked`
- [x] 6.5 `test/unit/verify-command.test.ts` rewritten: happy path (5 steps), missing-section failure, drifted-convention failure (naming the file), drifted-mission failure (naming the path), stop-after-first-failure, follow-a-skill-by-path. No new `integrity-checks.test.ts` cases were added as a separate task — instead verified via the real-CLI smoke test (task 10) exercising `doctor`/`lint`/`verify` together end to end; a dedicated unit-level `integrity-checks.test.ts` case for the new checks is reasonable follow-up but not required for this change's own test coverage, which the smoke test already exercises

## 7. Keep mission fresh on write

- [x] 7.1 `src/commands/rollup-write.ts`: after a content-changing write whose `relativePath` equals `store.config.organize.mission_path`, calls `buildAgentsMissionSection(store.root, store.config)` before returning
- [x] 7.2 Verified via the real-CLI smoke test (task 10.5: `ctxr rollup write MISSION.md ...` immediately reflected in `AGENTS.md`'s Mission section, no separate `ctxr update`) rather than a new unit test file — `rollup-write.ts` had no existing dedicated unit test file to extend, and the command-level behavior is exactly what the smoke test exercises

## 8. Pre-commit staleness guard

- [x] 8.1 DEVIATION from the original task text: `templates/hooks/pre-commit.sh` needed **no changes** — it already runs `ctxr doctor --staged --json` generically, so the new `stagedAgentsMdInlinedContentCurrentCheck` (task 6.2) plugs into the existing manifest (`INTEGRITY_CHECKS`) and is picked up automatically by the hook's existing invocation. Original task 8.1 mis-described the mechanism (assumed hook-script-level logic); the real fix lives entirely in the check registry
- [x] 8.2 Superseded by 6.2/6.5 — the staged check's behavior is what task 8 was actually asking for; no separate hook-harness test was added (would duplicate the check-level coverage)

## 9. Rendering fixes

- [x] 9.1 `src/core/agents-doc.ts`: `collapseNestedPrefixes` (new, private) drops any exclusion prefix already covered by an ancestor prefix (comparing on the trailing-slash-trimmed form, but rendering each surviving entry in its *original* form — the list mixes directory prefixes with bare file exclusions like `AGENTS.md`, so blindly appending `/` to every entry would have been wrong), sorts, and `renderLegRoutingSection` renders the result as one comma-joined inline-code line; `excludedPrefixesFor` itself untouched
- [x] 9.2 `templates/agents/retrieval-leg-routing.md`: confirmed no textual change needed — "scoped to exclude:" reads naturally into either a bullet list or the new single line
- [x] 9.3 `test/unit/agents-doc.test.ts`: leg-routing exact-output block updated for the new single-line format; added cases for nested-prefix collapsing and for a bare file exclusion (no trailing slash) rendering without one added

## 10. End-to-end verification

- [x] 10.1 `npm run typecheck && npm run build` — clean
- [x] 10.2 `npx vitest run --exclude '**/.claude/worktrees/**'` — 735/735 tests green across 88 files (one pre-existing test, `update-command.test.ts`, needed a one-line fix for a skill-index assertion it still carried)
- [x] 10.3 Fresh-store smoke test in a temp directory: `ctxr init --profile para`, added a convention file and `organize.mission_path` in `contexture.yaml`, ran `ctxr update` — confirmed section order (canonical → mission → retrieval → capture → placement → conventions), no skill index anywhere, inlined bodies with `_Source:_` provenance lines; a second `ctxr update` reported "Store is already up to date" (byte-identical, no write)
- [x] 10.4 Same store: `ctxr doctor` — only pre-existing, unrelated findings (`MISSION.md`'s own visibility/catalog-coverage, nothing to do with this change); `ctxr verify --portable` passed all 6 steps. Hand-edited the convention file directly (bypassing regeneration): both `ctxr doctor` and `ctxr verify --portable` failed, naming `.contexture/conventions/house-style.md` exactly; `ctxr update` repaired it and both passed again
- [x] 10.5 `ctxr rollup write MISSION.md --content-file <f>`: `AGENTS.md`'s Mission section reflected the new content immediately, with no separate `ctxr update` — and incidentally exercised the nested-fence-stripping path on a real `contexture:rollup` fence (not just the synthetic unit-test case), confirmed correct
- [x] 10.6 Interstitial-content case: hand-inserted a paragraph between the capture and placement sections, ran `ctxr update` — order left unchanged (verified: capture still precedes placement) and the hand-written paragraph preserved verbatim; `ctxr lint` reported `harness_portability.agents_md_section_order_blocked`; confirmed `ctxr doctor` does NOT report it (observation vs. invariant, kept separate as designed)
- [x] 10.7 `npm version minor --no-git-tag-version` (0.4.0 → 0.5.0, matching the pre-1.0 precedent set by `remove-agent-identity`/`generalize-identity-migration-residue` — additive-with-removals stays minor at this stage); `npm install --package-lock-only`; `src/version.ts` updated to match; `test/unit/version-sync.test.ts` green
