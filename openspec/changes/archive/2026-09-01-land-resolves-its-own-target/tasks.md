## 1. Target resolution

- [x] 1.1 `src/commands/session-land.ts`: replace `const branch = flags.branch ?? (await currentBranch(...))` with a resolution step that runs after the consent check and the forge lookup, and returns both the branch and where it came from: `--branch` wins; otherwise `--pr` defers resolution to the pull request read; otherwise `currentBranch(env.git, store.root)`. DEVIATION: no `'flag' | 'checkout' | 'pull-request'` enum — the two locally-known origins are treated identically everywhere (same guard, same mismatch check), so the origin is carried as `requested: string | undefined`, where `undefined` means "resolve from the pull request". Same behavior, one fewer concept.
- [x] 1.2 Same file: when the target came from `--branch` or the checkout, keep `SessionLandOnDefaultBranchError` firing before `configuredAdapters`/`isAvailable` is touched; when it came from `--pr`, read the pull request first and throw the same error on `pr.headBranch` before the gate. Apply `PullRequestHeadMismatchError` only for the `'flag'` and `'checkout'` origins
- [x] 1.3 `src/core/errors.ts`: reword `SessionLandOnDefaultBranchError`'s message so it reads correctly for a branch that came from a pull request rather than from the current checkout (name the branch and the refusal, not the checkout)
- [x] 1.4 `test/unit/session-land.test.ts`: add `--pr` with no `--branch` resolving the head branch and landing while the fake checkout is on `main`; add `--pr` naming a pull request whose head is `main` being refused after the read and before any merge call; leave the existing "refuses to land the default branch, before any forge call" case untouched — it must still pass with its `touched` assertion intact
- [x] 1.5 `npx vitest run test/unit/session-land.test.ts` — green, including the untouched pre-forge guard case

## 2. The canonical clone

- [x] 2.1 `src/core/git/worktree.ts`: add `mainWorktreePath(git: GitRunner, cwd: string): Promise<string>` returning the first entry of `listWorktrees` (git lists the main worktree first) and falling back to `cwd` when the list is empty
- [x] 2.2 `src/commands/session-land.ts`: resolve the clone path once and pass it to synchronization, which runs `currentBranch`, `hasRemote`, `fetchOrigin`, and `merge --ff-only` with `cwd` set to it; rename the function to say what it targets (`syncRootCheckout` → `syncCanonicalClone`) and reword its skip reasons to name the canonical clone and its path rather than "root checkout"
- [x] 2.3 Same file: change the reap self-reference guard from `path.resolve(target.path) === path.resolve(store.root)` to a check that `env.cwd` is the target worktree or lies inside it, with the refusal naming the canonical clone's path as where to retry; run `removeWorktree`/`deleteBranch` with `cwd` set to the clone
- [x] 2.4 `test/unit/session-land.test.ts`: update the "refuses to remove the worktree it is currently running from" case — it now asserts `sync: { attempted: true, synced: true }` against the canonical clone and only the reap skipped, with the message naming the clone path (this case previously pinned the defect); add a case landing from a *different* session worktree where the reap proceeds. Two further updates: the fixture now lists the clone AND the session worktree (a linked worktree's real `worktree list` always shows the main worktree first), which needed a cwd-aware fake git runner local to this file since the shared helper keys on argv alone; and "without --reap, touches no worktree" narrowed from "no `worktree` command at all" to "no `worktree remove`/`branch`", because locating the clone now always runs a read-only `worktree list --porcelain`
- [x] 2.5 `npx vitest run test/unit/session-land.test.ts` — green; confirm the fake git runner records `merge --ff-only origin/<default>` with the clone as its cwd, not the session worktree

## 3. Reporting

- [x] 3.1 `src/commands/session-land.ts`: in `outcome()`, print `skipped: <reason>` whenever a `SyncOutcome`/`ReapOutcome` carries a reason, reserving bare `not attempted` for the reasonless case; leave `SessionLandData`'s shape unchanged
- [x] 3.2 `test/unit/session-land.test.ts`: assert the human summary of a run that merged but skipped both steps names both reasons
- [x] 3.3 `npx vitest run test/unit/session-land.test.ts` — green

## 4. Skill prose

- [x] 4.1 `templates/skills/ctxr-land.md` (prose stays in the template, never in a TypeScript literal): step 1 gains that the target is named with `--branch <name>` or `--pr <n>` rather than inferred from the current checkout — the checkout you are standing in may be the canonical clone on `__DEFAULT_BRANCH__`, which has no session to infer; step 2 gains that merging and synchronizing work from any checkout and that synchronization fast-forwards the canonical clone; step 4 gains that `--reap` must run from outside the worktree being reaped, since no command can remove the directory it is running from, and that `git worktree list` names the canonical clone first
- [x] 4.2 `test/unit/skills.test.ts` and `test/integration/owned-skills.test.ts`: update the land-skill assertions to cover the two new rules (target named explicitly, reap runs from outside), following the existing per-skill assertion style rather than adding a parallel one. DEVIATION: `test/integration/owned-skills.test.ts` asserts delivery (every owned skill written with the managed header and indexed in AGENTS.md), not skill bodies — only `ctxr-placement` has a body assertion there, as a spot check. The two new rules are asserted where every other body rule already lives, in `test/unit/skills.test.ts`; the integration test needed no change and still passes
- [x] 4.3 `npx vitest run test/unit/skills.test.ts test/integration/owned-skills.test.ts` — green

## 5. End-to-end

- [x] 5.1 `npm run typecheck && npm run build && npx vitest run` — all clean, full suite green: 717/717 tests, 88/88 files (run with `--exclude '**/.claude/worktrees/**'`, the same nested-worktree test-discovery contamination the previous change recorded — this box keeps sibling worktrees under `.claude/worktrees/`)
- [x] 5.2 DEVIATION: verified against a scratch store whose `gh` is a scripted stub first on PATH — real `ctxr` binary, real git, real worktrees, real bare remote (the stub's `pr merge` fast-forwards the remote's default branch for real, so synchronization has actual work), but no GitHub account touched and no real pull request opened. From the canonical clone, `ctxr session land --pr 5 --yes --reap` with no `--branch` printed `PR #5 merged; sync ok; reap ok.` (exit 0); the clone's `main` moved `f6f5b22` → `ef01378` and `git worktree list` showed only the clone
- [x] 5.3 Same store, second throwaway session, same stub: from inside that session's worktree, `ctxr session land --pr 5 --yes --reap` printed `PR #5 merged; sync ok; reap skipped: cannot remove the worktree this command is running from; re-run from the canonical clone (<store>), or run \`ctxr session reap\` there.` — the clone's `main` advanced to `acd3c6a` (previously this invocation reported `sync not attempted`), the worktree survived, and `ctxr session reap` from the clone then reaped it (`reason: merged and clean`)
- [x] 5.4 `ctxr doctor --json` exits 0. For update: a store initialized by this build already ships the new prose, so the delivery path was exercised by first restoring the pre-change body into `.claude/skills/ctxr-land/SKILL.md` — the next `ctxr update --json` reported `changed: ['.claude/skills/ctxr-land/SKILL.md']` and the file came back with the new target/reap rules
- [x] 5.5 Bumped 0.4.0 → 0.4.1 via `npm version patch --no-git-tag-version` (behavior fix, no additions), synced `src/version.ts`, refreshed the lockfile with `npm install --package-lock-only`; `npx vitest run test/unit/version-sync.test.ts` green (2/2)
