# contexture store configuration (fixture: no schema_version field at all)

taxonomy:
  profile: para
  layers:
    - name: Projects
      path: projects
      description: Active work with a defined end state.
fields:
  visibility: scope
visibility:
  default_context: private
  directory_defaults: {}
derived:
  paths:
    - .contexture/
retrieval:
  exclude_paths:
    - .contexture/
    - identity/
