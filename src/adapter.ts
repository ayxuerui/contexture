/**
 * Public subpath for third-party adapters. Reserved in Phase 0 with just the
 * shared types; Phase 8.2 adds the registration contract and a
 * `defineAdapter()` helper alongside real discovery.
 */
export type { Adapter, AdapterKind } from './adapters/types.js';
