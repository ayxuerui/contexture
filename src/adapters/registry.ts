import type { Adapter, AdapterKind } from './types.js';

/** Stub: Phase 8.2 implements real discovery/registration from contexture.yaml. */
export function configuredAdapters(_kind: AdapterKind): readonly Adapter[] {
  return [];
}
